# Skill作成・更新依頼

## Goal

<反復可能にしたい作業と期待する成果物>

## Trigger examples

- 起動すべき依頼: `<具体例を2〜3件>`
- 起動すべきでない依頼: `<境界となる具体例を1〜2件>`
- 明示起動名: `$<skill-name>`

## Workflow

- 入力: `<ファイル、URL、値、前提状態>`
- 出力: `<成果物、変更、報告形式>`
- 必須手順: `<順序や検証>`
- 失敗時: `<停止条件、代替手段、承認が必要な操作>`

## Resources

- scripts: `<必要な決定的処理。なければ「なし」>`
- references: `<必要時だけ読む詳細資料。なければ「なし」>`
- assets: `<成果物へ使う素材。なければ「なし」>`
- tools / MCP: `<依存するツール。なければ「なし」>`

## Distribution

- 正本: `skills/<skill-name>/`
- 導入範囲: `<特定repo / ユーザー / 共有環境 / Plugin>`
- 互換性: `<対象OS、ランタイム、製品、既存Skillへの影響>`

## Done when

- `docs/SKILL_MANAGEMENT.md`の構成・作成・安全ルールを満たす。
- `python scripts/validate_repository.py`が成功する。
- 実行ごとに再生成する定型コードがある場合は、Skillの`scripts/`へ切り出されている。
- 追加したスクリプトと代表的な起動例を検証する。
- Skillカタログ、変更履歴、必要な公式資料とメモリが同期される。
- 変更内容、検証、導入方法、Git操作結果が報告される。
